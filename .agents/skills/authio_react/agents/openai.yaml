interface:
  display_name: "Authio React"
  short_description: "Repo-specific patterns and workflows for authio_react"
  default_prompt: "Use the authio_react repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true